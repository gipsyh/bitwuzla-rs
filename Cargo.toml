[package]
name = "bitwuzla-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
logicrs = { path = "../logicrs", version = "0.5.0" }

[build-dependencies]
giputils = { path = "../giputils", version = "0.3.3" }
